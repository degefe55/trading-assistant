# Local backups produced by scripts/backup_state.py — never commit
# (they may contain trade history, recommendation reasoning, etc).
backups/

# Local Obsidian-style notes vault — contains plaintext secrets
# (secrets.md.md) and personal trade journals. Never commit.
trading-notes/

# Claude Code local state (per-user settings, transcripts, etc).
.claude/

# Python bytecode caches.
__pycache__/
*.pyc
*.pyo
