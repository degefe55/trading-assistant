[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn webhook.app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 30"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
