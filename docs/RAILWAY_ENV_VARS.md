# Railway Environment Variables

Generated by grepping `os.getenv` / `os.environ` across the codebase. Names
only — never paste live values into this file.

Source files:
- `config.py` (the canonical list)
- `webhook/app.py` (Railway-runtime extras)
- `main.py` (cron-job dispatch)
- `core/commands.py` (read-only checks for status output)

---

## Required secrets

| Var | Used for |
|---|---|
| `ANTHROPIC_API_KEY` | Claude (Sonnet analyst, Haiku filter, watcher) calls. |
| `GOOGLE_SA_JSON` | Service-account JSON for Google Sheets read/write. Single-line JSON. |
| `GOOGLE_SHEET_URL` | The bot's Sheet (Positions, Logs, Recommendations, etc). |
| `TELEGRAM_BOT_TOKEN` | Bot identity for Telegram message send + webhook. |
| `TELEGRAM_CHAT_ID` | Where briefs and alerts are sent. |
| `TWELVE_DATA_KEY` | US equity prices, history, movers. |
| `MARKETAUX_KEY` | News headlines + entity-tagging for scout. |
| `FRED_KEY` | Macro data (Fed funds, 10Y, oil, CPI). |
| `SAHMK_API_KEY` | Saudi (Tadawul) prices via SAHMK API. |
| `DATABENTO_API_KEY` | CME ES futures (option-method runner). |
| `TRADINGVIEW_WEBHOOK_SECRET` | Shared secret for `/webhook/tradingview` Pine alerts. |
| `RUN_TOKEN` | Bearer token gating the manual `/run` HTTP endpoint. |

## Required (Railway-managed)

| Var | Used for |
|---|---|
| `PORT` | HTTP port the Flask app binds to. Set by Railway, not by you. |
| `PUBLIC_URL` | The public hostname Railway exposes. Used in Telegram webhook setup. |

## Mode + behavior toggles (have safe defaults)

| Var | Default | Purpose |
|---|---|---|
| `MOCK_MODE` | `true` | When true, the bot uses fake data — set `false` in production. |
| `DEBUG_MODE` | `true` | Verbose logs, prompt dumps, extra Telegram messages. |
| `DRY_RUN` | `false` | Stamp recommendations `[DRY RUN]` without acting on them. |
| `ACTIVE_MARKETS` | `US` | Comma-separated list of active markets (`US`, `SAUDI`, `US,SAUDI`). |
| `BRIEF_TIMEOUT_SEC` | `300` | Per-brief safety timeout in `webhook/app.py`. |
| `BRIEF_TYPE` | `test` | Which brief `main.py` runs when invoked as a one-shot (legacy GHA path). |
| `DATABENTO_DATASET` | `GLBX.MDP3` | Override only if a non-CME dataset is needed. |

## Cost controls

| Var | Default | Purpose |
|---|---|---|
| `DAILY_COST_ALERT_USD` | `1.00` | Alert threshold per day. |
| `MONTHLY_COST_CAP_USD` | `10.00` | Hard monthly cap (informational). |

## Logging

| Var | Default | Purpose |
|---|---|---|
| `MAX_LOG_ROWS` | `50000` | Auto-trim threshold for the Logs tab (10M cell ceiling defense). |
| `DIAGNOSTIC_AGENT_ENABLED` | `true` | Toggle the Haiku diagnostic agent (Phase G.5). |

## Currency

| Var | Default | Purpose |
|---|---|---|
| `USD_TO_SAR` | `3.75` | Display peg. SAR is pegged to USD; only override if the peg moves. |

## Watcher (Phase D.5 — always-on)

| Var | Default | Purpose |
|---|---|---|
| `WATCHER_ENABLED` | `true` | Master toggle. |
| `WATCHER_PRICE_INTERVAL_MIN` | `30` | Price-check cadence. |
| `WATCHER_NEWS_INTERVAL_MIN` | `60` | News-check cadence. |
| `WATCHER_INCLUDE_WATCHLIST` | `false` | Cover watchlist tickers, not just held positions. |
| `WATCHER_DAILY_ALERT_CAP` | `3` | Per-ticker per-day alert cap. |

## Friend forwarding (Phase G.5.2 — read-only method alerts)

| Var | Default | Purpose |
|---|---|---|
| `FRIEND_CHAT_ID` | (unset) | Optional. If set, forwards every method-flow alert (pre_signal, entry, tp1/2/3 hits, invalidated, setup_ended, user-cancel) to this Telegram chat for read-only review. Friend's chat is silent-dropped on any inbound message or button tap — they cannot run commands. Brief outputs, /buy /sell, watcher alerts, and error messages are NOT forwarded. Inline keyboards (the 🛑 cancel button on entry alerts) are stripped from the forwarded copy. |

## Method runner (Phase G.2 — option-method)

| Var | Default | Purpose |
|---|---|---|
| `METHOD_ENABLED` | `false` | Master toggle. Sheet `Config` tab wins at runtime. |
| `METHOD_INTERVAL_SEC` | `60` | Tick cadence. |
| `METHOD_TICKER` | `ES.FUT` | Databento parent symbol for the runner. |
| `METHOD_OPTION_PRICE_MIN` | `3.00` | (Wired in Phase G.3) options price-band floor. |
| `METHOD_OPTION_PRICE_MAX` | `3.90` | (Wired in Phase G.3) options price-band ceiling. |
| `METHOD_MAX_DAILY_SIGNALS` | `20` | Daily signal cap. |
| `METHOD_TELEGRAM_TP_HITS_ENABLED` | `false` | If `true`, fires Telegram alerts for `tp1_hit` / `tp2_hit` / `tp3_hit` AND `setup_ended` events. Default OFF (Phase G.5.3): friend's exit decisions happen on the option contract chart, so index-level TP pings and setup-end notices are noise. Sheet writes (`TP{1,2,3}Hit` flags, MethodSignals state transitions, MethodCooldown bumps) happen unconditionally — only Telegram is suppressed. Flip to `true` to restore the old behavior. |
